import { FilterUserAttributesPipe } from './filter-user-attributes.pipe';

describe('FilterUserAttributesPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterUserAttributesPipe();
    expect(pipe).toBeTruthy();
  });
});
