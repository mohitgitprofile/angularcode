import { PercentageMaskDirective } from './percentage-mask.directive';

describe('PercentageMaskDirective', () => {
  it('should create an instance', () => {
    const directive = new PercentageMaskDirective();
    expect(directive).toBeTruthy();
  });
});
